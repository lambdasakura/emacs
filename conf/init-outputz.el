;; outputz
;;(require 'outputz)
;;(setq outputz-key "9BakHjzsYRb.")      ;; 復活の呪文
;;(setq outputz-uri "http://works/%s")
;; 適当なURL。%sにmajor-modeの名前が入るので、major-modeごとのURLで投稿できます。
;;(global-outputz-mode nil)
;;(setq comment-style 'box)

;;(add-to-list 'outputz-modes 'lisp-mode )
